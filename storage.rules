rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // A user can only write to their own folder.
    match /{userId}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == userId;
      
      // Allow anyone to read the files.
      allow read;
    }
  }
}